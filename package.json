{
  "dependencies": {
    "dotenv": "^8.2.0",
    "if-env": "^1.0.4",
    "mongoose": "^6.3.8"
  },
  "name": "proxy",
  "version": "1.0.0",
  "main": "server/server.js",
  "devDependencies": {
    "concurrently": "^5.3.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/server.js",
    "start:prod": "cd server && npm start",
    "start:dev": "concurrently \"cd server && npm run watch\" \"cd client && npm start\"",
    "install": "npm init -y && npm install if-env && npm install -D concurrently && cd client && npm i && npm i apollo-boost graphql graphql-tag @apollo/react-hooks react-router-dom jwt-decode && cd ../server && npm i && npm i apollo-server-express jsonwebtoken",
    "seed": "cd server && npm run seed",
    "heroku-postbuild": "cd client && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/havena25/group-crit.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/havena25/group-crit/issues"
  },
  "homepage": "https://github.com/havena25/group-crit#readme",
  "keywords": [],
  "description": ""
}
